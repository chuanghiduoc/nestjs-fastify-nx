/**
 * Generated by orval 🍺
 * Do not edit manually.
 * NestJS Fastify Nx Boilerplate
 * Production-ready REST + GraphQL API built on NestJS, Fastify and Nx.
 *
 * ### Authentication
 * Session-based via [Better Auth](https://better-auth.com). Endpoints under `/api/auth/*` (tagged **auth**) cover sign-up, sign-in, sign-out, social providers, password reset and session lookup. Protected resource endpoints require the `better-auth.session_token` cookie — click **Authorize** and paste the value to try them out.
 *
 * ### Response shapes
 *
 * - **Success** — endpoints return the resource directly (Stripe-style: no envelope). List endpoints return a `ListResponseDto` with `object: "list"`, `data[]`, and `hasMore`.
 * - **Errors** — every error response uses [RFC 9457 Problem Details](https://www.rfc-editor.org/rfc/rfc9457) with `Content-Type: application/problem+json`. Branch on the `code` field (snake_case, stable) — never the human-readable `title`/`detail`.
 * - **Validation errors** carry an additional `errors[]` array with one entry per offending field; entries include `path`, `code`, `message`, `rule`, and `constraint`.
 *
 * ### Correlation
 *
 * Every response carries an `X-Request-Id` header (also mirrored as `requestId` in error bodies). Quote it when filing support tickets.
 * OpenAPI spec version: 1.0.0
 */
import type {
  CreateFeatureFlagDto,
  EvaluatedFlagsResponseDto,
  FeatureFlagResponseDto,
  FeatureFlagsList200,
  FeatureFlagsListParams,
  UpdateFeatureFlagDto,
} from './api.schemas';

import { customAxiosInstance } from '../lib/axios-instance';

export const getFeatureFlags = () => {
  /**
   * Cursor-paginated, newest first. Returns the flag definitions (master switch plus rollout share), not their resolution for a given subject — use `GET /feature-flags/evaluate` for that.
   * @summary List feature flags of the active organization
   */
  const featureFlagsList = (params?: FeatureFlagsListParams) => {
    return customAxiosInstance<FeatureFlagsList200>({
      url: `/api/v1/feature-flags`,
      method: 'GET',
      params,
    });
  };
  /**
   * @summary Create a feature flag
   */
  const featureFlagsCreate = (createFeatureFlagDto: CreateFeatureFlagDto) => {
    return customAxiosInstance<FeatureFlagResponseDto>({
      url: `/api/v1/feature-flags`,
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      data: createFeatureFlagDto,
    });
  };
  /**
   * Returns `{ key: boolean }` for every flag in the organization. Bucketing is deterministic: the subject is the calling user, or the API key when the caller is a machine, so the same subject always lands on the same side of a partial rollout.
   * @summary Resolve every flag for the calling subject
   */
  const featureFlagsEvaluate = () => {
    return customAxiosInstance<EvaluatedFlagsResponseDto>({
      url: `/api/v1/feature-flags/evaluate`,
      method: 'GET',
    });
  };
  /**
   * Only the fields present in the payload change. `key` is immutable.
   * @summary Update a feature flag
   */
  const featureFlagsUpdate = (id: string, updateFeatureFlagDto: UpdateFeatureFlagDto) => {
    return customAxiosInstance<FeatureFlagResponseDto>({
      url: `/api/v1/feature-flags/${id}`,
      method: 'PATCH',
      headers: { 'Content-Type': 'application/json' },
      data: updateFeatureFlagDto,
    });
  };
  /**
   * Clients checking the key stop seeing it in `evaluate` on the next call.
   * @summary Delete a feature flag
   */
  const featureFlagsRemove = (id: string) => {
    return customAxiosInstance<void>({ url: `/api/v1/feature-flags/${id}`, method: 'DELETE' });
  };
  return {
    featureFlagsList,
    featureFlagsCreate,
    featureFlagsEvaluate,
    featureFlagsUpdate,
    featureFlagsRemove,
  };
};
export type FeatureFlagsListResult = NonNullable<
  Awaited<ReturnType<ReturnType<typeof getFeatureFlags>['featureFlagsList']>>
>;
export type FeatureFlagsCreateResult = NonNullable<
  Awaited<ReturnType<ReturnType<typeof getFeatureFlags>['featureFlagsCreate']>>
>;
export type FeatureFlagsEvaluateResult = NonNullable<
  Awaited<ReturnType<ReturnType<typeof getFeatureFlags>['featureFlagsEvaluate']>>
>;
export type FeatureFlagsUpdateResult = NonNullable<
  Awaited<ReturnType<ReturnType<typeof getFeatureFlags>['featureFlagsUpdate']>>
>;
export type FeatureFlagsRemoveResult = NonNullable<
  Awaited<ReturnType<ReturnType<typeof getFeatureFlags>['featureFlagsRemove']>>
>;
