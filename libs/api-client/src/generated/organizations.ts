/**
 * Generated by orval 🍺
 * Do not edit manually.
 * NestJS Fastify Nx Boilerplate
 * Production-ready REST + GraphQL API built on NestJS, Fastify and Nx.
 *
 * ### Authentication
 * Session-based via [Better Auth](https://better-auth.com). Endpoints under `/api/auth/*` (tagged **auth**) cover sign-up, sign-in, sign-out, social providers, password reset and session lookup. Protected resource endpoints require the `better-auth.session_token` cookie — click **Authorize** and paste the value to try them out.
 *
 * ### Response shapes
 *
 * - **Success** — endpoints return the resource directly (Stripe-style: no envelope). List endpoints return a `ListResponseDto` with `object: "list"`, `data[]`, and `hasMore`.
 * - **Errors** — every error response uses [RFC 9457 Problem Details](https://www.rfc-editor.org/rfc/rfc9457) with `Content-Type: application/problem+json`. Branch on the `code` field (snake_case, stable) — never the human-readable `title`/`detail`.
 * - **Validation errors** carry an additional `errors[]` array with one entry per offending field; entries include `path`, `code`, `message`, `rule`, and `constraint`.
 *
 * ### Correlation
 *
 * Every response carries an `X-Request-Id` header (also mirrored as `requestId` in error bodies). Quote it when filing support tickets.
 * OpenAPI spec version: 1.0.0
 */
import type {
  CreateOrganizationRoleDto,
  CreateTeamDto,
  InvitationsList200,
  InvitationsListParams,
  ListResponseDto,
  OrganizationResponseDto,
  OrganizationRoleResponseDto,
  TeamResponseDto,
  TeamsList200,
  TeamsListParams,
  UpdateOrganizationRoleDto,
  UpdateTeamDto,
} from './api.schemas';

import { customAxiosInstance } from '../lib/axios-instance';

export const getOrganizations = () => {
  /**
   * Returns the organization the current session is scoped to, with member, team and pending-invitation counts. Switching organizations is done through the Better Auth surface (`POST /api/auth/organization/set-active`).
   * @summary Read the session's active organization
   */
  const organizationsCurrent = () => {
    return customAxiosInstance<OrganizationResponseDto>({
      url: `/api/v1/organizations/current`,
      method: 'GET',
    });
  };
  /**
   * Returns the built-in system roles followed by the roles this organization defined itself. System roles carry `system: true`, a null `id`, and cannot be updated or deleted. The list is small and bounded by the permission catalog, so it is not paginated.
   * @summary List roles available in the active organization
   */
  const organizationRolesList = () => {
    return customAxiosInstance<ListResponseDto>({
      url: `/api/v1/organizations/current/roles`,
      method: 'GET',
    });
  };
  /**
   * Creates a tenant-defined role carrying a subset of the permission catalog. The role is written to the same `organization_roles` table Better Auth reads, so a member assigned this role resolves the identical permissions through both the REST guard and the auth surface.
   * @summary Define a custom role for the active organization
   */
  const organizationRolesCreate = (createOrganizationRoleDto: CreateOrganizationRoleDto) => {
    return customAxiosInstance<OrganizationRoleResponseDto>({
      url: `/api/v1/organizations/current/roles`,
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      data: createOrganizationRoleDto,
    });
  };
  /**
   * Replaces the permission set wholesale — permissions omitted from the payload are revoked. System roles are not editable and answer 404.
   * @summary Replace the permissions of a custom role
   */
  const organizationRolesUpdate = (
    role: string,
    updateOrganizationRoleDto: UpdateOrganizationRoleDto,
  ) => {
    return customAxiosInstance<OrganizationRoleResponseDto>({
      url: `/api/v1/organizations/current/roles/${role}`,
      method: 'PATCH',
      headers: { 'Content-Type': 'application/json' },
      data: updateOrganizationRoleDto,
    });
  };
  /**
   * Refuses with 409 while any member still holds the role — deleting it would strip their permissions with no audit trail. Reassign those members first.
   * @summary Delete a custom role
   */
  const organizationRolesRemove = (role: string) => {
    return customAxiosInstance<void>({
      url: `/api/v1/organizations/current/roles/${role}`,
      method: 'DELETE',
    });
  };
  /**
   * Cursor-paginated, newest first. `search` matches the team name case-insensitively. Each entry carries the number of members currently assigned to the team.
   * @summary List teams in the active organization
   */
  const teamsList = (params?: TeamsListParams) => {
    return customAxiosInstance<TeamsList200>({
      url: `/api/v1/organizations/current/teams`,
      method: 'GET',
      params,
    });
  };
  /**
   * @summary Create a team in the active organization
   */
  const teamsCreate = (createTeamDto: CreateTeamDto) => {
    return customAxiosInstance<TeamResponseDto>({
      url: `/api/v1/organizations/current/teams`,
      method: 'POST',
      headers: { 'Content-Type': 'application/json' },
      data: createTeamDto,
    });
  };
  /**
   * @summary Rename a team
   */
  const teamsUpdate = (id: string, updateTeamDto: UpdateTeamDto) => {
    return customAxiosInstance<TeamResponseDto>({
      url: `/api/v1/organizations/current/teams/${id}`,
      method: 'PATCH',
      headers: { 'Content-Type': 'application/json' },
      data: updateTeamDto,
    });
  };
  /**
   * Team membership rows cascade with the team. Members keep their organization membership.
   * @summary Delete a team
   */
  const teamsRemove = (id: string) => {
    return customAxiosInstance<void>({
      url: `/api/v1/organizations/current/teams/${id}`,
      method: 'DELETE',
    });
  };
  /**
   * Cursor-paginated, newest first. Invitations are created through the Better Auth organization surface (`POST /api/auth/organization/invite-member`); this endpoint exposes them under the same list envelope and permission model as the rest of the API.
   * @summary List invitations issued by the active organization
   */
  const invitationsList = (params?: InvitationsListParams) => {
    return customAxiosInstance<InvitationsList200>({
      url: `/api/v1/organizations/current/invitations`,
      method: 'GET',
      params,
    });
  };
  /**
   * Moves a `pending` invitation to `canceled`. An invitation that was already accepted, rejected or canceled answers 409 — send a fresh invitation instead.
   * @summary Cancel a pending invitation
   */
  const invitationsCancel = (id: string) => {
    return customAxiosInstance<void>({
      url: `/api/v1/organizations/current/invitations/${id}`,
      method: 'DELETE',
    });
  };
  return {
    organizationsCurrent,
    organizationRolesList,
    organizationRolesCreate,
    organizationRolesUpdate,
    organizationRolesRemove,
    teamsList,
    teamsCreate,
    teamsUpdate,
    teamsRemove,
    invitationsList,
    invitationsCancel,
  };
};
export type OrganizationsCurrentResult = NonNullable<
  Awaited<ReturnType<ReturnType<typeof getOrganizations>['organizationsCurrent']>>
>;
export type OrganizationRolesListResult = NonNullable<
  Awaited<ReturnType<ReturnType<typeof getOrganizations>['organizationRolesList']>>
>;
export type OrganizationRolesCreateResult = NonNullable<
  Awaited<ReturnType<ReturnType<typeof getOrganizations>['organizationRolesCreate']>>
>;
export type OrganizationRolesUpdateResult = NonNullable<
  Awaited<ReturnType<ReturnType<typeof getOrganizations>['organizationRolesUpdate']>>
>;
export type OrganizationRolesRemoveResult = NonNullable<
  Awaited<ReturnType<ReturnType<typeof getOrganizations>['organizationRolesRemove']>>
>;
export type TeamsListResult = NonNullable<
  Awaited<ReturnType<ReturnType<typeof getOrganizations>['teamsList']>>
>;
export type TeamsCreateResult = NonNullable<
  Awaited<ReturnType<ReturnType<typeof getOrganizations>['teamsCreate']>>
>;
export type TeamsUpdateResult = NonNullable<
  Awaited<ReturnType<ReturnType<typeof getOrganizations>['teamsUpdate']>>
>;
export type TeamsRemoveResult = NonNullable<
  Awaited<ReturnType<ReturnType<typeof getOrganizations>['teamsRemove']>>
>;
export type InvitationsListResult = NonNullable<
  Awaited<ReturnType<ReturnType<typeof getOrganizations>['invitationsList']>>
>;
export type InvitationsCancelResult = NonNullable<
  Awaited<ReturnType<ReturnType<typeof getOrganizations>['invitationsCancel']>>
>;
