# pnpm 11 reads only auth/registry settings from .npmrc; every other setting moved to
# pnpm-workspace.yaml (strictPeerDependencies, autoInstallPeers, minimumReleaseAge, …).
# Kept as an empty placeholder so the Dockerfile `COPY .npmrc*` layer still matches.
